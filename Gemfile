# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/ #{repo_name}" }

# Development dependencies for editor LSP support
group :development do
  gem 'rubocop'
  gem 'solargraph'
  gem 'yard'
end
